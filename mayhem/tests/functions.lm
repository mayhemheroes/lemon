def fib(var n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
print(fib(10));
def make_adder(var n) {
    def adder(var x) {
        return x + n;
    }
    return adder;
}
var add5 = make_adder(5);
print(add5(10));
